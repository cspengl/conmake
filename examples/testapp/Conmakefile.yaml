---
version: v1
project: testapp

steps:
  build:
    workstation: building
    script:
      - ls -al /
      - gcc -static -o testapp testapp.c
  run:
    workstation: running
    script:
      - ls -al
      - ./testapp

workstations:
  building:
    base: gcc:latest
    autoinit: true
    preparation:
      - touch /test
  running:
    base: ubuntu:18.04
    autoinit: true
    preparation:
...
